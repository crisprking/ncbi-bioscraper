# Direct dependencies imported by the notebook.
# Versions chosen for compatibility with Colab's preinstalled stack as
# of 2026-05; bump as needed when Colab updates.
biopython>=1.83
requests>=2.31
pandas>=2.0
tqdm>=4.66
pypdf>=4.0
lxml>=4.9
# Optional — only needed if GROQ_SUMMARIES=True
# groq>=0.5
